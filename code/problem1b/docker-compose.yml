services:
  minimal-polynomial-finder:
    build:
      context: .
      dockerfile: Dockerfile
    image: minimal-polynomial-finder:latest
    container_name: minimal-polynomial-finder
    platform: linux/amd64
    # Remove container after execution
    restart: "no"
    # If you want to run interactively, uncomment the following:
    # stdin_open: true
    # tty: true
    # command: /bin/bash

